-- Initial data for Account Selling Platform

-- Set search path
SET search_path TO account_selling, public;

-- Insert roles (will be used once Role entity is created)
-- INSERT INTO roles (id, name) VALUES (1, 'ROLE_USER');
-- INSERT INTO roles (id, name) VALUES (2, 'ROLE_ADMIN');

-- Note: Uncomment and modify these inserts once the corresponding entities are created