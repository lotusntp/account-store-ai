spring:
  application:
    name: account-selling-platform-test

  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password: 
    driver-class-name: org.h2.Driver
  flyway:
    enabled: false
  sql:
    init:
      mode: never
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    database-platform: org.hibernate.dialect.H2Dialect

management:
  tracing:
    enabled: true
    sampling:
      probability: 1.0
  otlp:
    tracing:
      endpoint: http://localhost:4318/v1/traces

logging:
  level:
    root: INFO
    com.accountselling.platform: DEBUG
    org.springframework: INFO
  elasticsearch:
    host: localhost
    port: 5000

otel:
  sdk:
    disabled: false
  traces:
    exporter: none
  logs:
    exporter: none

jwt:
  secret: dGhpc19pc19hX3NlY3VyZV9zZWNyZXRfa2V5X2Zvcl9qd3RfdG9rZW5zX3RoYXRfaXNfbG9uZ19lbm91Z2g
  refresh-expiration: 3600000 # 1 ชั่วโมง (ms) หรือปรับตามโค้ดจริง
  expiration: 3600000
